let valor = 5;
let elemento = decodeURIComponent.getElementbyId("status")

if (valor === 10) {
    elemento.innerText = "A condição é verdadeira!"
} else {
    elemento.innerText = "A condição é falsa"
}